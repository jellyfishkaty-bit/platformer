import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gamerplatformer.app',
  appName: 'Геймер',
  webDir: 'dist',
  backgroundColor: '#0a0a0f',
  android: {
    backgroundColor: '#0a0a0f',
  },
};

export default config;
